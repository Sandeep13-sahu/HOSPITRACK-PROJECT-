spring:
  datasource:
    url: jdbc:h2:mem:hospitrack_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: hospitrack_super_secret_jwt_signing_key_at_least_256_bits_length_for_hmac_sha256_security_production_2026
    expiration-ms: 3600000
    refresh-expiration-ms: 86400000
  cors:
    allowed-origins: http://localhost:8000
  brevo:
    api-key: mock-brevo-key
    sender-email: test@hospitrack.com
    sender-name: Test Hospitrack
  admin:
    initial-email: admin@hospitrack.com
    initial-password: Admin@Hospitrack2026!
